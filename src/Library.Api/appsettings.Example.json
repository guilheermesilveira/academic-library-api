{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "server={localhost};port={port};user={user};password={password};database={name_db}"
  },
  "JwtSettings": {
    "ExpirationHours": 2,
    "PathOfKeys": "enter a path on your computer to store the secret keys"
  },
  "StorageSettings": {
    "ImageFolderPath": "enter a path on your computer to store the images"
  },
  "AllowedHosts": "*"
}